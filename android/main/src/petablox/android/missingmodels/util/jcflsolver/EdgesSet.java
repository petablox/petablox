package petablox.android.missingmodels.util.jcflsolver;

public class EdgesSet extends MyHashSet<Edge> implements Edges
{
	public Edge addEdge(Edge edge)
	{
		throw new RuntimeException("This is broken. Fix me if you use me!");
	}
}
